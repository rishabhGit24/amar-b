# AMAR Generated Application

This React application was automatically generated by AMAR (Autonomous Memory Agentic Realms).

## Pages

- HomePage (/): Main landing page showcasing the coffee shop's ambiance, offerings, and key information.
- MenuPage (/menu): Detailed display of all coffee, food, and beverage items with descriptions and prices.
- AboutPage (/about): Page telling the coffee shop's story, philosophy, sourcing, and introducing the team.
- VisitUsPage (/visit-us): Provides location, operating hours, directions, map, and a contact form for inquiries.

## Components

- Header: Top navigation bar with branding, responsive menu, and links to main sections.
- Footer: Standard footer containing copyright information, social media links, and quick navigation.
- HeroSection: Prominent full-width section with a captivating image, shop name, and a call-to-action.
- CallToActionSection: Section encouraging visitors to explore menu or visit the shop with a clear call to action.
- MenuSection: Displays categorized menu items like coffees, pastries, and food with details and pricing.
- AboutSection: Presents the coffee shop's unique story, values, and commitment to quality ingredients.
- TeamSection: Showcases key team members, their roles, and a brief bio or philosophy.
- LocationSection: Shows the shop's address, detailed operating hours, parking information, and an embedded map.
- ContactFormSection: Interactive form allowing customers to send messages or inquiries directly to the shop.

## Backend API

This application includes a Node.js/Express backend with the following API endpoints:

- **POST /api/contact**: Handles submissions from the contact form.

### Backend Configuration

The backend server runs on port 3001 by default. You can configure this in the `.env` file.

### API Response Format

All API endpoints return JSON responses in the following format:

```json
{
  "success": true,
  "message": "Operation completed successfully",
  "data": {}
}
```

Error responses include:
```json
{
  "success": false,
  "message": "Error description"
}
```

## Getting Started

### Prerequisites

- Node.js 14+ and npm

### Installation

1. Install dependencies:
   ```bash
   npm install
   ```

2. Create a `.env` file based on `.env.example` (for backend configuration)

### Running the Application

#### Frontend Only

```bash
npm start
```

Open [http://localhost:3000](http://localhost:3000) to view it in the browser.

### Running Frontend and Backend Together

```bash
npm run dev
```

This will start both the React frontend (port 3000) and Express backend (port 3001) concurrently.

### Running Backend Only

```bash
npm run start:backend
```

Or with auto-reload during development:

```bash
npm run dev:backend
```

### Testing Backend

```bash
npm run test:backend
```


## Available Scripts

- `npm start` - Runs the React app in development mode
- `npm test` - Launches the frontend test runner
- `npm run build` - Builds the app for production
- `npm run dev` - Runs both frontend and backend concurrently
- `npm run start:backend` - Runs the backend server only
- `npm run test:backend` - Runs backend API tests

## Project Structure

```
.
â”œâ”€â”€ src/
â”‚   â”œâ”€â”€ components/     # Reusable React components
â”‚   â”œâ”€â”€ pages/          # Page components
â”‚   â”œâ”€â”€ App.tsx         # Main app component with routing
â”‚   â””â”€â”€ index.tsx       # Entry point
â”œâ”€â”€ api/              # Backend API handlers
â”œâ”€â”€ tests/            # Backend tests
â”œâ”€â”€ server.js         # Express server
â””â”€â”€ package.json
```

## Generated by AMAR

This application was generated automatically based on the following plan:
- **Complexity**: simple
- **Pages**: 4
- **Components**: 9
- **Backend Logic**: Yes
- **API Endpoints**: 1

## Learn More

- [React Documentation](https://reactjs.org/)
- [React Router Documentation](https://reactrouter.com/)
- [Express Documentation](https://expressjs.com/)
