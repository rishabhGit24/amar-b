import React from 'react';

const ContactForm: React.FC = ({}) => {
  return (
    <div className="contactform">
      <h2>ContactForm</h2>
      <p>An interactive form within the Contact Section allowing users to submit messages or inquiries directly to the cafe. It typically includes fields for name, email, and message.</p>
    </div>
  );
};

export default ContactForm;
